<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Smart Attendance – Faculty System</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
:root{
  --blue:#2563eb;
  --bg:#f5f7fb;
  --card:#ffffff;
  --muted:#64748b;
  --green:#22c55e;
}
*{box-sizing:border-box;font-family:Inter,system-ui}
body{margin:0;background:var(--bg)}
.container{max-width:1400px;margin:auto;padding:20px}
.hidden{display:none}

/* NAV */
nav{
  display:flex;justify-content:space-between;align-items:center;
  background:#fff;padding:14px 20px;border-radius:12px
}
nav a{
  color:#334155;text-decoration:none;font-weight:500;
  margin-right:20px;cursor:pointer
}
nav a.active{color:var(--blue)}
nav input{padding:8px 12px;border-radius:8px;border:1px solid #e2e8f0}

/* COMMON */
.card{
  background:var(--card);padding:16px;border-radius:14px;
  box-shadow:0 6px 18px rgba(0,0,0,.05)
}
.btn{
  padding:8px 14px;border-radius:8px;border:1px solid #e2e8f0;
  background:#fff;cursor:pointer
}
.btn.primary{
  background:var(--blue);color:#fff;border:none
}
h2{margin:0}
p{color:var(--muted)}

/* STATS */
.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:16px;margin-top:20px}
.stat-num{font-size:28px;font-weight:700}

/* GRID */
.grid{display:grid;grid-template-columns:320px 1fr;gap:20px;margin-top:20px}

/* TABLE */
table{width:100%;border-collapse:collapse}
th,td{padding:12px;text-align:left}
th{color:var(--muted)}
tr:not(:last-child){border-bottom:1px solid #eef2f7}

/* FORMS */
input,select{
  width:100%;padding:10px;border-radius:8px;
  border:1px solid #e2e8f0;margin-top:6px
}

footer{
  margin-top:30px;color:#94a3b8;
  display:flex;justify-content:space-between;font-size:13px
}
</style>
</head>

<body>
<div class="container">

<!-- NAV -->
<nav>
  <div>
    <strong>⭐ Smart Attendance</strong>
    <a class="active" onclick="showPage('dashboard',this)">Dashboard</a>
    <a onclick="showPage('classes',this)">Classes</a>
    <a onclick="showPage('reports',this)">Reports</a>
    <a onclick="showPage('settings',this)">Settings</a>
  </div>
  <div>
    <input placeholder="Search…" oninput="globalSearch(this.value)">
    <button class="btn" onclick="logout()">Logout</button>
  </div>
</nav>

<!-- DASHBOARD -->
<section id="dashboard">
  <h2>Faculty Dashboard</h2>
  <p>Real-time attendance monitoring</p>

  <div class="stats">
    <div class="card"><p>Total Enrolled</p><div class="stat-num">—</div></div>
    <div class="card"><p>Present Now</p><div class="stat-num">—</div></div>
    <div class="card"><p>Avg Attendance</p><div class="stat-num">—</div></div>
    <div class="card"><p>Late Arrivals</p><div class="stat-num">—</div></div>
  </div>

  <div class="grid">
    <div class="card">
      <h3>Session Controls</h3>
      <label>Subject</label>
      <select><option>— Select Subject —</option></select>
      <label>Classroom</label>
      <input placeholder="e.g. L-402">
      <br><br>
      <button class="btn primary" onclick="toggleSession()">Start Session</button>
    </div>

    <div class="card">
      <h3>Live Attendance</h3>
      <p>No active records</p>
      <table>
        <thead>
          <tr><th>Student</th><th>ID</th><th>Time</th><th>Method</th><th>Status</th></tr>
        </thead>
        <tbody></tbody>
      </table>
      <br>
      <button class="btn" onclick="exportCSV()">Export CSV</button>
      <button class="btn" onclick="window.print()">Print</button>
    </div>
  </div>
</section>

<!-- CLASSES -->
<section id="classes" class="hidden">
  <h2>Classes</h2>
  <p>Manage assigned classes</p>

  <div class="card">
    <button class="btn primary" onclick="addClass()">+ Add Class</button>
    <table>
      <thead>
        <tr><th>Class Code</th><th>Subject</th><th>Room</th><th>Status</th></tr>
      </thead>
      <tbody id="classTable">
        <tr><td colspan="4" style="color:#94a3b8">No classes loaded</td></tr>
      </tbody>
    </table>
  </div>
</section>

<!-- REPORTS -->
<section id="reports" class="hidden">
  <h2>Reports</h2>
  <p>Generate attendance reports</p>

  <div class="card">
    <label>From Date</label>
    <input type="date">
    <label>To Date</label>
    <input type="date">
    <br><br>
    <button class="btn primary">Generate Report</button>
    <button class="btn">Export</button>
    <p style="margin-top:20px;color:#94a3b8">No report generated</p>
  </div>
</section>

<!-- SETTINGS -->
<section id="settings" class="hidden">
  <h2>Settings</h2>
  <p>Faculty profile & system preferences</p>

  <div class="card">
    <label>Name</label>
    <input placeholder="Faculty Name">
    <label>Email</label>
    <input placeholder="email@college.edu">
    <label>Department</label>
    <input placeholder="Department">
    <br><br>
    <button class="btn primary" onclick="saveSettings()">Save Settings</button>
  </div>
</section>

<footer>
  <span>© 2026 Smart Attendance System</span>
  <span>Support • Security • System Health</span>
</footer>

</div>

<script>
let sessionActive=false;

function showPage(id,el){
  document.querySelectorAll("section").forEach(s=>s.classList.add("hidden"));
  document.getElementById(id).classList.remove("hidden");
  document.querySelectorAll("nav a").forEach(a=>a.classList.remove("active"));
  el.classList.add("active");
}

function toggleSession(){
  sessionActive=!sessionActive;
  alert(sessionActive?"Session Started":"Session Stopped");
}

function exportCSV(){
  alert("CSV export triggered (DB data ready)");
}

function addClass(){
  const table=document.getElementById("classTable");
  table.innerHTML+=`
  <tr>
    <td>—</td><td>—</td><td>—</td><td>Inactive</td>
  </tr>`;
}

function saveSettings(){
  alert("Settings saved");
}

function logout(){
  alert("Logged out");
  location.reload();
}

function globalSearch(q){
  console.log("Search query:",q);
}
</script>

</body>
</html>
